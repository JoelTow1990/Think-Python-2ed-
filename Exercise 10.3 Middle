/* Exercise 10.3. Write a function called middle that takes a list and returns a new list that contains
all but the first and last elements. For example:
>>> t = [1, 2, 3, 4]
>>> middle(t)
[2, 3] */

def middle(t):
    """ assumes that t is a list, returns a list containing all the elements 
    of t except the first and last elements."""
    return t[1:len(t)-1]
